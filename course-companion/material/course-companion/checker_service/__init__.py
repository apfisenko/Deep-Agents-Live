"""checker_service — сервис проверки домашек (отдельная деплой-единица).

Второй пакет того же репозитория, но ДРУГАЯ сервисная граница: у чекера свой
граф в langgraph.json (co-deployed) или целиком свой сервер со своим конфигом
`langgraph.checker.json` (распил). От пакета companion не зависит — общего кода
через границу сервисов нет, только контракт Agent Protocol (state с `messages`).
"""
