# AI Homework Mentor

Консольная утилита (Rich CLI) на DeepAgents для проверки домашних заданий.

Точка входа на Windows: `.\make.ps1` (`sync` / `run` / `lint` / `format` / `test`).

Документы: [concept/](concept/), [roadmap.md](roadmap.md).
